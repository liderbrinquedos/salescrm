import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { Toaster as SonnerToaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Brinquedos CRM - Sistema para Representantes",
  description: "Sistema de gestão de vendas de brinquedos para representantes comerciais com multitenancy, catálogo de produtos, clientes e pedidos.",
  keywords: ["Brinquedos", "CRM", "Vendas", "Representantes", "Next.js", "TypeScript", "Prisma"],
  authors: [{ name: "Brinquedos CRM Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "Brinquedos CRM - Sistema para Representantes",
    description: "Sistema de gestão de vendas de brinquedos para representantes comerciais",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
        <SonnerToaster />
      </body>
    </html>
  );
}
